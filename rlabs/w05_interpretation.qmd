```{r}
lavaanPlot(model = fit0, coefs = TRUE, stand = TRUE, stars = "regress")
```


```{r}
lavaanPlot(model = fit1, coefs = TRUE, stand = TRUE, stars = "regress")
```

```{r}
lavaanPlot(model = fit2, coefs = TRUE, stand = TRUE, stars = "regress")
```